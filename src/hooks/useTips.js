import { useEffect, useState } from "react";


export const useTips = ( menu = [] ) => {

    try {
        
    } catch (error) {
        
    }
    
  return {
    ...tipState,
    tipState,
    onInputChange
  }
}
